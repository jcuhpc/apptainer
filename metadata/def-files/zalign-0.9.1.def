Bootstrap: localimage
From: /gpfs01/sw/containers/ubuntu-22.04.sif

%runscript
exec "$@"

%post
apt-get update  &&  \
apt-get install -y zalign  &&  \
apt-get clean  &&  apt-get purge  &&  rm -rf /var/lib/apt/lists/*

